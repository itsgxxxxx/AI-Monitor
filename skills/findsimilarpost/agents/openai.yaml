interface:
  display_name: "Find Similar Post"
  short_description: "分析英文推文并检索中文区同项目高流量帖子，给出发送建议。"
  default_prompt: "Use $findsimilarpost to analyze this tweet URL and return the send decision."

policy:
  allow_implicit_invocation: true
